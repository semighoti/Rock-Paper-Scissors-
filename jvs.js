const button = document.querySelectorAl("button");

const getBotChoice = () => ["rock", "paper", `scissors`][Math.floor(Math.random() * 3)];

buttons.forEach(button => {
  button.addEventListener("click", () => {

  })

});